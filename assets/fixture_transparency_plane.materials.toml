# Fixture: Transparency Plane Sweep — material library
#
# OpenPBR-native signals only. This fixture starts with opacity-only sweeps.

model = "openpbr"
colorspace = "lin_rec709_scene"

[CoveragePlane]
base_color = [0.85, 0.85, 0.90]
specular_weight = 0.0
geometry_opacity = 0.5

[MatteRed]
base_color = [0.55, 0.06, 0.05]
specular_weight = 0.0
