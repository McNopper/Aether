# Fixture: Rect Light — material library

model = "openpbr"
colorspace = "lin_rec709_scene"

[Floor]
base_color    = [0.500, 0.500, 0.500]
specular_weight = 0.0

[Matte]
# Neutral diffuse receiver — validates irradiance without specular interference.
base_color    = [0.700, 0.700, 0.700]
specular_weight = 0.0

[RectLight]
# 5000 cd/m² white rect light.
emission_color     = [1.000, 1.000, 1.000]
emission_luminance = 5000.0
