# Fixture: Point Light — material library

model = "openpbr"
colorspace = "lin_rec709_scene"

[Floor]
base_color    = [0.500, 0.500, 0.500]
specular_weight = 0.0

[Matte]
# Neutral diffuse receiver.
base_color    = [0.700, 0.700, 0.700]
specular_weight = 0.0

[PointLight]
# 20 000 cd/m² — small sphere radius means low radiant flux; adjust luminance
# if the scene appears over/under-exposed.
emission_color     = [1.000, 1.000, 1.000]
emission_luminance = 20000.0
